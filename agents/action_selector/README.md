# 动作选择器 Agent

根据对话历史和用户最新输入，从动作库中选择最合适的动作。

## 功能

- 分析对话上下文和用户输入
- 从预定义动作库中选择动作
- 仅输出动作+句号，不做任何解释

## 动作库

愤怒, 生气, 难过, 委屈, 道歉, 感动, 惊讶, 惊吓, 吃惊, 疑惑, 思考, 自豪, 骄傲, 自信, 平静, 微笑, 害羞, 羞涩, 大笑

## 输出格式

```
动作。
```

示例：`平静。`

## 使用

```bash
python -m src eval --agent action_selector --limit 3
```
